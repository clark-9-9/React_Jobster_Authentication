const BadRequest = require("./bad-request");
const NotFound = require("./not-found");
const UnAuthenticate = require("./unAuthenticate");

module.exports = {
    BadRequest, NotFound, UnAuthenticate
}